# Dependencies

## Runtime
- Nessuna dipendenza obbligatoria lato runtime per `fuckadblock.js`.

## Tooling
- `npm` / `bower` solo per distribuzione pacchetto (opzionale).

## Licenze
- Progetto rilasciato con licenza MIT (vedi `LICENSE`).
